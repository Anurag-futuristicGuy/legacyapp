{
    "parser": "@typescript-eslint/parser",  // Specifies the TypeScript parser
    "parserOptions": {
      "ecmaVersion": 2020,                  // Supports modern JavaScript syntax
      "sourceType": "module",               // Allows `import` statements
      "project": "./tsconfig.json"          // Points to your TypeScript config file
    },
    "extends": [
      "eslint:recommended",                 // Extends ESLint's recommended rules
      "plugin:@typescript-eslint/recommended", // Extends TypeScript linting rules
      "next/core-web-vitals"               // Extends Next.js linting rules
    ],
    "plugins": [
      "@typescript-eslint"                 // Uses the TypeScript plugin
    ],
    "rules": {
      // You can add or override custom ESLint rules here
    }
  }
  